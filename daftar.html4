<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Portal Resmi - Mitra Nusantara</title>

  <!-- Meta Tags untuk Preview Gambar saat link dibagikan -->
  <meta property="og:title" content="Mitra Nusantara - Platform Investasi Terpercaya">
  <meta property="og:description" content="Terdaftar dan diawasi oleh OJK. Gabung sekarang dan bangun aset digital Anda bersama Mitra Nusantara.">
  <meta property="og:image" content="https://i.ibb.co/L0S7xYy/logo-mitra.png"> 
  <meta property="og:url" content="https://k38973087-boop.github.io/Undangan-digital-/">
  <meta property="og:type" content="website">

  <!-- Firebase SDK -->
  <script src="https://www.gstatic.com/firebasejs/9.17.1/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.17.1/firebase-database-compat.js"></script>
  <!-- FontAwesome Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; }
    
    /* Background Premium Fintech - Menyesuaikan Skema Warna Logo */
    body { 
      background: linear-gradient(135deg, #02153b 0%, #002b80 60%, #0057d9 100%);
      background-attachment: fixed;
      color: #333; 
      display: flex; 
      flex-direction: column; 
      align-items: center; 
      min-height: 100vh; 
      padding: 20px 0; 
    }
    
    /* OJK & Trust Banner (Dibuat semi-transparan mewah di atas background gelap) */
    .ojk-banner { background: rgba(255, 255, 255, 0.95); border: 1px solid rgba(255,255,255,0.2); border-radius: 12px; width: 100%; max-width: 420px; padding: 15px; display: flex; align-items: center; justify-content: space-between; margin-bottom: 15px; box-shadow: 0 4px 20px rgba(0,0,0,0.15); backdrop-filter: blur(10px); }
    .ojk-info h4 { font-size: 13px; color: #002b80; font-weight: bold; }
    .ojk-info p { font-size: 11px; color: #64748b; margin-top: 2px; }
    .ojk-badge { background: #b91c1c; color: white; padding: 4px 8px; border-radius: 4px; font-weight: 800; font-size: 12px; letter-spacing: 0.5px; }

    /* Security Trust Badges Line */
    .trust-line { display: flex; justify-content: space-around; width: 100%; max-width: 420px; font-size: 11px; color: #cbd5e1; margin-bottom: 20px; font-weight: 500; }
    .trust-line span { display: flex; align-items: center; gap: 5px; }
    .trust-line i { color: #10b981; }

    /* Main Container Card */
    .portal-card { background: #ffffff; width: 100%; max-width: 420px; border-radius: 20px; padding: 35px 25px; box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3); border: 1px solid rgba(255, 255, 255, 0.1); text-align: center; }
    
    /* Logo Brand Area - Menggunakan Aset Gambar Resmi Anda */
    .logo-container { display: flex; flex-direction: column; align-items: center; margin-bottom: 25px; }
    .brand-logo-img { height: 60px; max-width: 100%; object-fit: contain; margin-bottom: 15px; }
    .brand-title { font-size: 24px; font-weight: 800; color: #002b80; letter-spacing: -0.5px; }
    .brand-subtitle { font-size: 13px; color: #64748b; margin-top: 4px; }

    /* Auth Navigation Tabs (Tombol Login/Daftar) */
    .auth-tabs { display: flex; background: #f1f5f9; padding: 4px; border-radius: 8px; margin-bottom: 25px; }
    .tab-btn { flex: 1; border: none; background: none; padding: 10px; font-size: 13px; font-weight: 700; color: #64748b; border-radius: 6px; cursor: pointer; transition: 0.2s; }
    .tab-btn.active { background: #ffffff; color: #002b80; box-shadow: 0 2px 6px rgba(0,0,0,0.05); }

    /* Form Fields */
    .form-group { margin-bottom: 18px; text-align: left; }
    .form-group label { display: block; font-size: 12px; font-weight: 700; color: #1e293b; margin-bottom: 6px; }
    
    .input-wrapper { display: flex; align-items: center; background: #f8fafc; border: 1px solid #cbd5e1; border-radius: 8px; padding: 0 12px; transition: 0.2s; }
    .input-wrapper:focus-within { border-color: #0057d9; background: #ffffff; box-shadow: 0 0 0 3px rgba(0, 87, 217, 0.1); }
    .input-wrapper span { font-size: 14px; font-weight: bold; color: #475569; padding-right: 8px; border-right: 1px solid #cbd5e1; margin-right: 10px; }
    .input-wrapper input { width: 100%; border: none; background: transparent; padding: 12px 0; font-size: 14px; color: #1e293b; outline: none; }
    
    /* Captcha Section */
    .captcha-container { display: flex; gap: 10px; align-items: center; margin-top: 5px; }
    .captcha-box { background: linear-gradient(135deg, #e2e8f0, #cbd5e1); color: #002b80; font-weight: 800; font-size: 18px; letter-spacing: 4px; padding: 10px 15px; border-radius: 6px; font-style: italic; user-select: none; border: 1px dashed #94a3b8; }
    .btn-refresh { background: none; border: none; color: #0057d9; font-size: 18px; cursor: pointer; padding: 5px; transition: 0.2s; }
    .btn-refresh:hover { transform: rotate(180deg); }

    /* Button Action */
    .btn-submit { width: 100%; padding: 14px; background: #0057d9; color: white; border: none; border-radius: 8px; cursor: pointer; font-weight: bold; font-size: 14px; margin-top: 10px; transition: 0.2s; box-shadow: 0 4px 12px rgba(0, 87, 217, 0.2); }
    .btn-submit:hover { background: #002b80; }
    .btn-submit:disabled { background: #94a3b8; cursor: not-allowed; box-shadow: none; }

    .hidden { display: none; }

    /* Security Badges Footer */
    .security-footer { text-align: center; width: 100%; max-width: 420px; margin-top: 30px; padding-top: 20px; border-top: 1px solid rgba(255,255,255,0.1); }
    .badge-grid { display: flex; justify-content: center; gap: 15px; font-size: 11px; color: #cbd5e1; font-weight: bold; margin-bottom: 15px; }
    .badge-grid span { display: flex; align-items: center; gap: 4px; border: 1px solid rgba(255,255,255,0.15); padding: 5px 10px; border-radius: 6px; background: rgba(255,255,255,0.05); }
    .badge-grid i { color: #3b82f6; }
    
    .legal-text { font-size: 11px; color: #94a3b8; line-height: 1.6; margin-top: 10px; }
  </style>
</head>
<body>

  <!-- OJK Verification Header Banner -->
  <div class="ojk-banner">
    <div class="ojk-info">
      <h4>Terdaftar & Diawasi OJK</h4>
      <p>No. Registrasi: AEP-4300/2024</p>
    </div>
    <div class="ojk-badge">OJK</div>
  </div>

  <!-- Trust Features -->
  <div class="trust-line">
    <span><i class="fas fa-shield-alt"></i> Aman Terpercaya</span>
    <span>|</span>
    <span><i class="fas fa-user-shield"></i> Diawasi OJK</span>
    <span>|</span>
    <span><i class="fas fa-lock"></i> Data Terenkripsi</span>
  </div>

  <!-- Main Portal Card -->
  <div class="portal-card">
    
    <!-- Logo Resmi Mitra Nusantara -->
    <div class="logo-container">
      <img src="1781556657113.jpg" alt="Logo Mitra Nusantara" class="brand-logo-img">
      <div class="brand-title">Mitra Nusantara</div>
      <div class="brand-subtitle" id="portal-desc">Buat akun Mitra Nusantara Anda sekarang</div>
    </div>

    <!-- Tombol Navigasi Antara Login & Daftar (Tab Switcher) -->
    <div class="auth-tabs">
      <button class="tab-btn active" id="tab-register" onclick="switchMode('register')">Daftar Baru</button>
      <button class="tab-btn" id="tab-login" onclick="switchMode('login')">Masuk Akun</button>
    </div>

    <!-- Form Container -->
    <div id="portal-form">
      <!-- Phone Input -->
      <div class="form-group">
        <label for="phone">Nomor Telepon</label>
        <div class="input-wrapper">
          <span>+62</span>
          <input type="text" id="phone" placeholder="81234567890" autocomplete="off">
        </div>
      </div>

      <!-- Password Input -->
      <div class="form-group">
        <label for="pass">Kata Sandi</label>
        <div class="input-wrapper">
          <input type="password" id="pass" placeholder="Masukkan kata sandi">
        </div>
      </div>

      <!-- Confirm Password Input (Hanya untuk register) -->
      <div class="form-group" id="group-confirm">
        <label for="confirmPass">Konfirmasi Kata Sandi</label>
        <div class="input-wrapper">
          <input type="password" id="confirmPass" placeholder="Ulangi kata sandi Anda">
        </div>
      </div>

      <!-- Referral Input (Hanya untuk register) -->
      <div class="form-group" id="group-referral">
        <label for="upline">Kode Referral (Opsional)</label>
        <div class="input-wrapper">
          <input type="text" id="upline" placeholder="Masukkan kode referral">
        </div>
      </div>

      <!-- Captcha Verification (Hanya untuk register) -->
      <div class="form-group" id="group-captcha">
        <label for="captcha-input">CAPTCHA Verification</label>
        <div class="captcha-container">
          <div class="captcha-box" id="captcha-code">0000</div>
          <button class="btn-refresh" type="button" onclick="refreshCaptcha()"><i class="fas fa-sync-alt"></i></button>
        </div>
        <div class="input-wrapper" style="margin-top: 8px;">
          <input type="text" id="captcha-input" placeholder="Masukkan kode di atas">
        </div>
      </div>

      <!-- Submit Action Button -->
      <button class="btn-submit" id="btn-submit" onclick="handleSubmit()">Daftar Sekarang</button>
    </div>

  </div>

  <!-- Compliance Footer Badges -->
  <div class="security-footer">
    <div class="badge-grid">
      <span><i class="fas fa-certificate"></i> SSL Secure</span>
      <span><i class="fas fa-key"></i> 256-bit Encryption</span>
      <span><i class="fas fa-check-circle"></i> Verified</span>
    </div>
    
    <div class="legal-text">
      <strong>Customer Support:</strong> 24/7 Siap Membantu<br>
      © 2026 Mitra Nusantara Capital. Semua hak dilindungi.<br>
      PT. Mitra Nusantara Capital terdaftar dan diawasi oleh OJK berlandaskan nomor izin registrasi regulasi KEP-45/D.05/2024.
    </div>
  </div>

  <script>
    // Inisialisasi Layanan Database Firebase Anda
    const firebaseConfig = { databaseURL: "https://admin-panel-5c204-default-rtdb.firebaseio.com/" };
    firebase.initializeApp(firebaseConfig);
    const db = firebase.database();

    let currentMode = "register"; 
    let currentCaptcha = "";

    // Pembuat Kode Keamanan CAPTCHA Otomatis
    function refreshCaptcha() {
      const chars = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ";
      let code = "";
      for (let i = 0; i < 4; i++) {
        code += chars.charAt(Math.floor(Math.random() * chars.length));
      }
      currentCaptcha = code;
      document.getElementById('captcha-code').innerText = code;
      document.getElementById('captcha-input').value = "";
    }

    // Fungsi Switcher Mode Form (Login / Daftar)
    function switchMode(mode) {
      currentMode = mode;
      
      document.getElementById('tab-register').classList.toggle('active', mode === 'register');
      document.getElementById('tab-login').classList.toggle('active', mode === 'login');

      document.getElementById('group-confirm').classList.toggle('hidden', mode === 'login');
      document.getElementById('group-referral').classList.toggle('hidden', mode === 'login');
      document.getElementById('group-captcha').classList.toggle('hidden', mode === 'login');

      if (mode === 'register') {
        document.getElementById('portal-desc').innerText = "Buat akun Mitra Nusantara Anda sekarang";
        document.getElementById('btn-submit').innerText = "Daftar Sekarang";
        document.getElementById('pass').placeholder = "Min 8 karakter (Huruf besar, kecil, angka)";
        refreshCaptcha();
      } else {
        document.getElementById('portal-desc').innerText = "Silakan masuk ke dalam dashboard investasi Anda";
        document.getElementById('btn-submit').innerText = "Masuk Sekarang";
        document.getElementById('pass').placeholder = "Masukkan kata sandi Anda";
      }
    }

    window.onload = function() {
      refreshCaptcha();
    };

    function handleSubmit() {
      if (currentMode === "register") {
        executeRegister();
      } else {
        executeLogin();
      }
    }

    // Eksekusi Pendaftaran Akun Ke Firebase & Admin Panel
    function executeRegister() {
      const phone = document.getElementById('phone').value.trim();
      const pass = document.getElementById('pass').value.trim();
      const confirmPass = document.getElementById('confirmPass').value.trim();
      const uplineCode = document.getElementById('upline').value.trim();
      const captchaInput = document.getElementById('captcha-input').value.trim().toUpperCase();
      const btn = document.getElementById('btn-submit');

      if (phone.length < 9 || phone.length > 13 || isNaN(phone) || !phone.startsWith('8')) {
        return alert("Pendaftaran Gagal: Gunakan format nomor HP Indonesia yang benar (Contoh: 81234567890 tanpa angka 0 di depan).");
      }

      const passwordRegex = /^(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}$/;
      if (!passwordRegex.test(pass)) {
        return alert("Keamanan Lemah: Kata sandi minimal 8 karakter dan wajib dikombinasikan antara huruf besar, huruf kecil, serta angka.");
      }

      if (pass !== confirmPass) {
        return alert("Pendaftaran Gagal: Konfirmasi kata sandi tidak cocok. Harap periksa kembali.");
      }

      if (captchaInput !== currentCaptcha) {
        alert("Verifikasi Gagal: Kode CAPTCHA yang Anda masukkan salah.");
        refreshCaptcha();
        return;
      }

      btn.disabled = true;
      btn.innerText = "Memproses Keamanan...";

      db.ref('users/' + phone).once('value').then(snapshot => {
        if (snapshot.exists()) {
          alert("Pendaftaran Gagal: Nomor telepon ini sudah terdaftar di sistem kami.");
          btn.disabled = false;
          btn.innerText = "Daftar Sekarang";
          refreshCaptcha();
        } else {
          const dataPayload = {
            phone: phone,
            password: pass,
            email: "-", 
            points: 5000, 
            level: "Gratis",
            upline: uplineCode || "-",
            joined: new Date().toISOString()
          };

          db.ref('users/' + phone).set(dataPayload).then(() => {
            localStorage.setItem('n15_user', phone);
            window.location.href = 'beranda.html'; 
          }).catch(error => {
            alert("Kesalahan Sistem: " + error.message);
            btn.disabled = false;
            btn.innerText = "Daftar Sekarang";
          });
        }
      });
    }

    // Eksekusi Login Akun
    function executeLogin() {
      const phone = document.getElementById('phone').value.trim();
      const pass = document.getElementById('pass').value.trim();
      const btn = document.getElementById('btn-submit');

      if (!phone || !pass) {
        return alert("Gagal Masuk: Harap isi Nomor Telepon dan Kata Sandi Anda.");
      }

      btn.disabled = true;
      btn.innerText = "Memvalidasi Akun...";

      db.ref('users/' + phone).once('value').then(snapshot => {
        if (snapshot.exists() && snapshot.val().password === pass) {
          localStorage.setItem('n15_user', phone);
          window.location.href = 'beranda.html'; 
        } else {
          alert("Gagal Masuk: Nomor telepon atau kata sandi Anda salah.");
          btn.disabled = false;
          btn.innerText = "Masuk Sekarang";
        }
      }).catch(error => {
        alert("Kesalahan Jaringan: " + error.message);
        btn.disabled = false;
        btn.innerText = "Masuk Sekarang";
      });
    }
  </script>
</body>
  </html>
